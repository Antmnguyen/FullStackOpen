```mermaid
sequenceDiagram
participant browser
participant server
    browser->>server: POST JSON FILE
    note right of browser: sends JSON file of new note to server
    activate server
    server-->>browser: 201 response
    deactivate server
    note right of server: updates current page
```
